node('Siteo') {
    stage('Preparation') {
        checkout scm
    }
    stage('Build'){
        sh 'npm install'
        sh 'npm run build'
    }
    stage('Server'){
        sh 'npm run server'
    }
}